using Microsoft.AspNetCore.Mvc;
using PersonalSystemProcedures.Models.MobileModels;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace PersonalSystemProcedures.Interfaces
{
    public interface ILoginRepository
    {
        /*
        List<UserInfo> Login(UserLogin item);
        bool BuildUser([FromBody] UserBuild item);
        bool UpdateInfo(UserInfo item);
        void BuidUserHP();
        bool UpdateUserHP(UserHPortrait item);
        */
    }
}
